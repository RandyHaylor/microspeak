The compliance export finished for the Ontario tenants on the 14th, roughly 80 percent of their records. Quebec tenants were skipped entirely because their retention policy is different and we have not decided how to handle it. The export was validated against the sample checksums but not against the full set, so I would not call it verified. Priyanka wrote the export script, not me, I only ran it. The one issue we hit was the CSV header emitting 'client_id' where downstream expects 'clientId', and that is corrected now. The archive job that runs after the export was never re-enabled and is still paused.
